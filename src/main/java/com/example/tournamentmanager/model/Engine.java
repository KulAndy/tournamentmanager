package com.example.tournamentmanager.model;


public interface Engine {
    static int generatePairing(Tournament tournament, boolean reversColors) {
        return 0;
    }

    static void checkPairing(Tournament tournament, byte round) {
    }

    static Tournament generateRandomTournament() {
        return null;
    }
}
